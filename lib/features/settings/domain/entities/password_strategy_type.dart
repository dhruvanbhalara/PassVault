/// Defines the type of password generation strategy.
enum PasswordStrategyType {
  /// Random character-based generation.
  random,

  /// Wordlist-based passphrase generation (memorable).
  memorable,
}
